/**
 * Shared constants for CSharp Painkiller extension.
 */

// ============================================================================
// File extensions and patterns (actively used)
// ============================================================================

/** C# file extension */
export const CS_EXTENSION = '.cs';

/** Suffix for handler classes */
export const HANDLER_SUFFIX = 'Handler';
